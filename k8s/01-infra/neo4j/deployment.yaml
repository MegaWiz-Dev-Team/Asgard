# Neo4j — Graph database for knowledge graphs
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: neo4j-data
  namespace: asgard-infra
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: neo4j
  namespace: asgard-infra
  labels:
    app: neo4j
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: neo4j
  template:
    metadata:
      labels:
        app: neo4j
    spec:
      containers:
        - name: neo4j
          image: neo4j:5-community
          ports:
            - containerPort: 7474
            - containerPort: 7687
          env:
            - name: NEO4J_AUTH
              valueFrom:
                secretKeyRef:
                  name: neo4j-secret
                  key: NEO4J_AUTH
            - name: NEO4J_PLUGINS
              value: '["apoc"]'
            - name: NEO4J_server_config_strict__validation_enabled
              value: "false"
            - name: NEO4J_server_memory_heap_initial__size
              value: "2G"
            - name: NEO4J_server_memory_heap_max__size
              value: "4G"
            - name: NEO4J_server_memory_pagecache_size
              value: "2G"
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              memory: 2Gi
              cpu: 500m
            limits:
              memory: 7Gi
              cpu: 2000m
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: neo4j-data
---
apiVersion: v1
kind: Service
metadata:
  name: neo4j
  namespace: asgard-infra
spec:
  selector:
    app: neo4j
  type: NodePort
  ports:
    - name: browser
      port: 7474
      targetPort: 7474
      nodePort: 30474
    - name: bolt
      port: 7687
      targetPort: 7687
      nodePort: 30687
---
# NOTE: This Secret is no longer applied from git. Create out-of-band:
#
#   NEO4J_PW=$(kubectl get secret asgard-secrets -n asgard -o jsonpath='{.data.NEO4J_PASSWORD}' | base64 -d)
#   kubectl create secret generic neo4j-secret -n asgard-infra \
#     --from-literal=NEO4J_AUTH="neo4j/${NEO4J_PW}"
#
# Kept here as a documented schema only. See docs/security/SECRETS.md.
# Reconcile script: ./scripts/reconcile-neo4j-secret.sh
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: neo4j-secret
#   namespace: asgard-infra
# type: Opaque
# stringData:
#   NEO4J_AUTH: neo4j/<REDACTED — see asgard-secrets:NEO4J_PASSWORD>
